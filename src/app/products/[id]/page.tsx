import React from 'react';


const ProductDetail = () => {
  return (
    <div>Product Details
      {/* {products.map((product) => (
        <div key={product.id}>
          <h3>{product.name}</h3>
          <p>Price: ${product.price}</p>
        </div>
      ))} */}
    </div>
  );
};

export default ProductDetail;
